TAGT.models.Integrations = new class extends TAGT.models._Base
	constructor: ->
		@_initModel 'integrations'


	# FIND
	# findByRole: (role, options) ->
	# 	query =
	# 		roles: role

	# 	return @find query, options

	# CREATE
